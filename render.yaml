services:
  - type: web
    name: ferrite-backend
    runtime: docker
    dockerfilePath: ./server/Dockerfile
    dockerContext: ./server
    envVars:
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production
    plan: free
    healthCheckPath: /api/health
